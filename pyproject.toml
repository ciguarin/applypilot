[project]
name = "applypilot"
version = "1.5.2"
description = "AI-powered job application pipeline — Canadian internship edition"
readme = "README.md"
license = "AGPL-3.0-only"
requires-python = ">=3.11"
authors = [{name = "Ian Guarin"}]
keywords = ["job-search", "automation", "ai", "resume", "apply", "job-application", "canada", "internship"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Environment :: Console",
    "License :: OSI Approved :: GNU Affero General Public License v3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business",
]

dependencies = [
    "typer>=0.9.0",
    "rich>=15.0.0",
    "httpx>=0.24",
    "beautifulsoup4>=4.12",
    "playwright>=1.40",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
    "pypdf>=4.0",
    ]

[project.optional-dependencies]
dev = ["pytest>=7.0", "ruff>=0.1"]

[project.scripts]
applypilot = "applypilot.cli:app"

[project.urls]
Homepage = "https://github.com/ciguarin/applypilot"
Repository = "https://github.com/ciguarin/applypilot"
Issues = "https://github.com/ciguarin/applypilot/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/applypilot"]

[tool.hatch.build]
artifacts = [
    "src/applypilot/config/*.yaml",
    "src/applypilot/config/*.json",
    "src/applypilot/config/.env.example",
    "src/applypilot/scripts/*",
    "src/applypilot/scripts/launchagents/*",
]

[tool.ruff]
target-version = "py311"
line-length = 120
